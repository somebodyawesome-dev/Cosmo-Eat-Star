using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[CreateAssetMenu(fileName = "AuthData", menuName = "PlayerData")]

public class AuthData : ScriptableObject
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

   
}
